import { Image } from 'react-native'

export default {
  remove: Image.resolveAssetSource(require('../../assets/icons/remove.png')),
  back: Image.resolveAssetSource(require('../../assets/icons/back.png')),
  avatar: Image.resolveAssetSource(require('../../assets/icons/avatar.jpeg')),
}
